def diagonalDifference(arr):
    # Write your code here 
    f,s=0,0
    for i in range(len(arr)):
        f+=arr[i][i]
    arr.reverse()
    for i in range(len(arr)):
        s+=arr[i][i]
    return abs(s-f)
